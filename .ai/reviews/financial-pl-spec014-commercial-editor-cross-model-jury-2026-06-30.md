# Cross-model review record — SPEC-014 (financial_pl commercial-grade invoice editor)

- **Date:** 2026-06-30 · **Branch:** `feat/financial-pl-ksef` · **Scope:** `git diff --staged` (the SPEC-014 change — 19 files, ~1414 lines; unrelated `yarn.lock`/`apps/sandbox` dep churn excluded).
- **Jury:** Claude fresh-reviewer (mandatory, in-family) + Codex gpt-5.5 xhigh + Kimi K2.7 Thinking + DeepSeek V4 Pro max. Code-stage, 2 rounds. (SPEC-014's spec-stage jury ran in the prior session — see the SPEC-014 changelog: Codex+DeepSeek confirmed, Kimi skipped at spec stage.)
- **Criteria:** the SPEC-014 acceptance criteria (buyer→`metadata.buyerSnapshot`, NIP lookup proxy, inline validations, VAT/unit pickers, GTU/procedure search, BC additivity) + the repo's own `om-code-review` checklist (auto-injected by each wrapper).

## Code-stage round 1
- **Claude fresh-reviewer** `pass` (0 blockers) — verified the buyer-snapshot↔`buildBuyer` 1:1 key contract, fail-open lookup (auth+feature gate, 400 bad NIP, 200 `{ok:false}` on upstream failure, `accountNumbers` dropped), all inline validations, the stale-closure-safe async merge, BC additivity, and DS/i18n. 5 benign notes (framework-level feature gate per convention; `parseWykazAddress` puts street in line1 by design; PL-default country with a content-gated detail card; `.mercato` regenerated by `yarn generate`; customer combobox degrades gracefully).
- **DeepSeek V4 Pro** `pass` (0 blockers) — 4 benign notes (NIP-invalid early feedback; picker custom→standard snap on a matching numeric; legacy `metadata.buyer` key preserved on re-save (harmless — `buyerSnapshot` wins); customer-combobox placeholder unchanged when the feature is absent).
- **Kimi K2.7** `fail` (1 blocker) — **(high)** picking VAT "Other…" and leaving the custom input blank passes save validation (`if (rateText)` guard) → the line silently persists as 0% VAT, violating acceptance criterion #3. Notes: MF Wykaz `date` derived from UTC not Europe/Warsaw; `buyerToSnapshot({countryCode:'PL'})` treated as non-empty; OSS country combobox free-form?; BuyerFields `htmlFor` ↔ ComboboxInput id association.
- **Codex gpt-5.5 xhigh** `fail` (2 blockers) — **(correctness)** the NIP-lookup response is applied without re-checking that the current NIP still matches the request's NIP: change the NIP mid-flight and the previous NIP's name/address/VAT fill against the now-different NIP → a `buyerSnapshot` whose company details don't match its NIP. **(requirement)** same blank-"Other…" VAT → silent 0% as Kimi.

### Reconciliation (3 confirmed fixes; flagged blocker by ≥2 voters investigated first)
1. **Blank "Other…" line VAT → silent 0%** (Codex + Kimi — strong signal). **Fixed** `InvoiceForm.tsx`: every line now requires a non-empty numeric VAT rate (`!rateText || !Number.isFinite(rate) || rate<0 || rate>100` throws `vatRateNumeric`). A real 0% line is the explicit "0%" pick; a blank custom rate is rejected before save.
2. **Stale NIP-lookup race** (Codex). **Fixed** `BuyerFields.tsx`: `runLookup` captures `requestedNip` and discards the response if `valueRef.current.nip` no longer normalises to it — the previous NIP's data can no longer fill against a changed NIP.
3. **`buyerToSnapshot({countryCode:'PL'})` non-empty** (Kimi note → hardened). **Fixed** `buyer-snapshot.ts`: the mapper now omits the snapshot unless an *identifying* field (name/NIP/address/postal/city) is present, so a never-filled buyer never persists a meaningless `{countryCode}`; +2 unit assertions.
- **Logged, not fixed (non-blocking):** UTC vs Europe/Warsaw `date` for the Wykaz lookup (UTC date is always ≤ Warsaw date → never a future-date rejection; at most queries the prior day's register, immaterial for a VAT-status check); legacy `metadata.buyer` key preserved on re-save (`buyerSnapshot` takes precedence in `buyerFromMetadata`, so harmless); OSS country combobox is restricted (no `allowCustomValues`, unlike the buyer-name combobox); DeepSeek's "NIP invalid on any digits" — refuted (the code gates on `nipDigits.length>0 && !isValidPolishNip`).

## Code-stage round 2 (re-review the fixed diff)
- **Codex** `fail` (1 NEW blocker — convergent, not a loop) — **(correctness)** the taxpayer NIP (`contextNip`) was checksum-validated on *normalised* digits in `InvoiceForm`, but `buildMetaPayload` persisted it **raw**; a dashed `525-234-40-78` (which the client accepts) then 422'd against `invoiceMetaPutSchema.contextNip = z.string().regex(/^[0-9]{10}$/)`. Secondary: a buyer NIP of `ABC` normalises to `''`, skips validation, and `buyerToSnapshot` silently drops it. **Confirmed real (verified the schema) → fixed:** `buildMetaPayload` normalises `contextNip` to bare digits (or null); buyer + taxpayer validators now reject a non-empty field that isn't a valid NIP (raw-content check), so garbage is rejected not dropped; inline `nipInvalid` aligned. Codex note (lookup response shape defined locally vs `z.infer` from validators) logged — non-runtime, the route's zod response schema is explicit.
- **Kimi** round-2 retry hit the documented print-mode parse gotcha (`skipped`); re-run cleanly on the final diff (below).
- **Polish (DeepSeek note, not a blocker):** added inline taxpayer-NIP checksum feedback in `PlVatMetaForm` (parity with the buyer NIP field; reuses the existing `validation.nipChecksumTaxpayer` key — no i18n churn).

## Final round — all reviewers on the fully-fixed diff
- **Claude fresh-reviewer** `pass` (0 blockers) — adversarially re-verified every round-1/round-2 fix: blank-"Other…" VAT throws (never persists 0%); NIP normalisation consistent across all three write paths (`buildMetaPayload`, `buyerToSnapshot`, validators) so a dashed taxpayer NIP no longer 422s; the async race is handled twice over (response discarded on NIP change + blank-only merge); lookup route gating/fail-open/no-`accountNumbers`; buyer→`metadata.buyerSnapshot` exact-key contract + no-clobber merge.
- **Codex gpt-5.5 xhigh** `pass` (0 blockers, 0 notes) — round-2 taxpayer-NIP blocker confirmed resolved.
- **DeepSeek V4 Pro** `pass` (0 blockers) — 3 benign notes (taxpayer inline feedback — since ADDED; NIP placeholder format example hardcoded; VAT-status badge persists until next lookup).
- **Kimi K2.7 Thinking** `pass` (0 blockers) — the wrapper's auto-scraper hit the documented kimi-cli print-mode gotcha (two consecutive `skipped`s); re-run directly with raw-output capture, Kimi's verdict object is `{"verdict":"pass","blockers":[],"notes":[]}`.

### Final reconciliation
Every reproducible blocker raised across the jury (Kimi ×1, Codex ×3 over two rounds) was independently confirmed and fixed; the blank-"Other…" VAT blocker was raised by two voters (Codex + Kimi). No spurious blocker was chased. Notes logged: MF Wykaz `date` uses UTC (always ≤ Europe/Warsaw date → never a future-date rejection; immaterial for a VAT-status check); legacy `metadata.buyer` key preserved on re-save (`buyerSnapshot` wins in `buyerFromMetadata` → harmless); NIP/postal/country placeholders are format examples; VAT-status badge persistence. **Code-stage cross-model: confirmed (claude + codex + deepseek + kimi) — all four reviewers `pass` on the final fully-fixed diff.**

## Verification (after fixes)
- build:packages **PASS** · i18n:check-sync **PASS** · module jest **400 passed / 10 skipped** · our-source `tsc` **0 errors** (18 pre-existing vendored `@open-mercato/ui/DataTable.tsx` errors are NOT this change).
- Live KSeF TEST (2026-06-30): VAT/KOR/ZAL/UPR/OSS accepted + UPO; self-billed rejected 410 (guard); certificate (XAdES) auth accepted. MF Wykaz NIP lookup live-verified (`5252344078` → Google Poland, Czynny).

**Verdict: ready — stop-before-PR.** All four reviewers `pass` on the final fully-fixed diff (mandatory Claude + Codex + DeepSeek + Kimi); all 6 reproducible blockers fixed and re-confirmed; gate green (build:packages · i18n · jest 400 · tsc 0). Live KSeF TEST + cert auth re-verified.
