# Ephemeral generators output (entities.ids, entity-fields-registry, etc.).
# Regenerated by `yarn generate` in the host monorepo on every run, and
# would be wiped by a `clean-generated` pass anyway. See the host repo's
# AGENTS.md "Generated Files: versioned vs ephemeral" section for the
# convention this mirrors.
generated/

# esbuild output
dist/

# Jest cache
.jest-cache/
